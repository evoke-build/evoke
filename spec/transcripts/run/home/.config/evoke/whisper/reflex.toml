reflex = 1

description = """
Whisper what the program was told.
An argv body for the spec: a script under the home's bin, the input and a setting from the environment."""
effect = "read"
confirm = "Whisper it?"
run = ["whisper"]

[config]
greeting = "How to whisper"

[examples]
"whisper hello there" = {}
