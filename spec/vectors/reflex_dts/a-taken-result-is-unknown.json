{
  "input": {
    "manifest": {
      "description": "Find the release most likely behind a failure.\nWeighs a service's errors, its deploys and its logs, and names the release.",
      "not_for": [
        "listing deploys",
        "rolling a release back"
      ],
      "tags": [
        "ops"
      ],
      "effect": "read",
      "confirm": "Find the suspect?",
      "run": "suspect.mts",
      "config": {},
      "args": {},
      "yields": {
        "release": "quoted"
      },
      "takes": {
        "errors": "errors",
        "deploys": "deploys",
        "logs": "logs"
      },
      "examples": {
        "find the suspect": {},
        "which deploy broke it": {},
        "find the release behind the outage": {}
      },
      "tests": {
        "what broke checkout": {},
        "list the deploys": false
      },
      "unknown": []
    }
  },
  "expect": "// Generated by evoke check; do not edit.\n\nexport interface Args {\n  /** The whole result of an earlier step that returns errors. */\n  errors: unknown\n  /** The whole result of an earlier step that returns deploys. */\n  deploys: unknown\n  /** The whole result of an earlier step that returns logs. */\n  logs: unknown\n}\n\nexport interface Config {}\n\n/** What the body receives beside its arguments. */\nexport interface Context {\n  /** The input as it was typed. */\n  input: string\n  config: Config\n  /** Aborted at the deadline or on a decline: stop, never guess. */\n  signal: AbortSignal\n}\n\n/** What the body returns: the text a person reads, and data an app may use. */\nexport type Result = string | { text: string; data?: unknown }\n\n/** The body: the default export of the file `run` names. */\nexport type Reflex = (args: Args, context: Context) => Result | Promise<Result>\n"
}
