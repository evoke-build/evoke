{
  "input": {
    "locked": { "writes": ["~/Downloads"], "runs": ["open"] },
    "upstream": { "writes": ["~/Desktop"], "runs": ["open"] }
  },
  "expect": {
    "type": "needs_accept",
    "locked": { "runs": ["open"] },
    "upstream": { "writes": ["~/Desktop"], "runs": ["open"] }
  }
}
